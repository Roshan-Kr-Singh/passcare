import React, { Component } from "react";
 
class Stuff extends Component {
  render() {
    return (
      <div>
        <h2>ABOUT</h2>
        <p>This document describes the problems with the existing password management methods, 
          and lists the required characteristics of the new system. Nowadays almost every web or
          mobile based service requires some kind of username and password based login system. 
          Memorizing the login credentials for each platform is not feasible.
        </p>
        
      </div>
    );
  }
}
 
export default Stuff;